<h1><%= @song.name%></h1>
<h3><%= link_to Artist.find(@song.artist_id).name, artist_path(Artist.find(@song.artist_id)) %></h3>
<h5><%= link_to Genre.find(@song.genre_id).name, genre_path(Genre.find(@song.genre_id)) %></h5>